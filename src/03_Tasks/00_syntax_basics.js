// Beispielobjekt: Informationen über Google
const google = {
    address: {
        street: '1600 Amphitheatre Parkway',
        city: 'Mountain View',
        state: 'CA',
        printAddress () {
            return `${this.street}, ${this.city}, ${this.state}`
        }
    },
    search: 'https://www.google.com/',
    docs: 'https://docs.google.com/',
    maps: 'https://maps.google.com/',
}

// Greife auf die Stadt von Google zu und lass sie in der Konsole ausgeben


// Führe die Funktion printAddress aus


// Füge eine neue Eigenschaft zu google hinzu, die auf die URL von Google Mail verweist


// Erstelle eine Funktion die zwei Übergabeparameter erhält und diese in der Konsole ausgibt


// Du hast eine Variable X, die zufällig einen Wert zwischen 1 und einschließlich 10 enthält
// Ist X kleiner als 5, befülle Y mit false, ansonsten mit true
// Schreibe dies einmal als if-else und einmal als ternary operator
const x = Math.floor(Math.random() * 10) + 1




// Iteriere über folgendes Array und gib alle Werte aus, die größer oder gleich 5 sind
const numbers = [1, 2, 3, 4, 5, 6, 7, 8, 9]





